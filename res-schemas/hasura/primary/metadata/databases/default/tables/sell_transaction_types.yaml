table:
  name: transaction_types
  schema: sell
is_enum: true
array_relationships:
  - name: transactions
    using:
      foreign_key_constraint_on:
        column: type
        table:
          name: transactions
          schema: sell
