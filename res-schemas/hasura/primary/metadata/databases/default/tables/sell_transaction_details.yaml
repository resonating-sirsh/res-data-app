table:
  name: transaction_details
  schema: sell
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - make_cost
        - order_date
        - order_number
        - order_type
        - price
        - revenue_share
        - sku
        - total_amount
        - transaction_id
        - type
      filter: {}
      allow_aggregations: true
