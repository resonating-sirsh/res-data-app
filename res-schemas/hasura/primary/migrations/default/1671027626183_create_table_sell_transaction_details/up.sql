CREATE TABLE "sell"."transaction_details" ("id" serial NOT NULL, "sku" text NOT NULL, "make_cost" float4 NOT NULL, "price" float4 NOT NULL, "shipping" float4 NOT NULL, "transaction_id" integer NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("transaction_id") REFERENCES "sell"."transactions"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"), UNIQUE ("sku"));COMMENT ON TABLE "sell"."transaction_details" IS E'This table holds Finance state for each item at the moment of payment. ';
