CREATE TABLE "make"."assemble_rolls" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "roll_type" text NOT NULL, "roll_length" numeric NOT NULL, "roll_key" text NOT NULL, "material_name_match" text NOT NULL, "roll_id" integer NOT NULL, "material" text NOT NULL, "purchasing_record_id" text NOT NULL, "_warehouse_rack_location" text, "location_id" text, "po_number" text, "standard_cost_per_unit" numeric, "metadata" jsonb NOT NULL DEFAULT jsonb_build_object(), "created_at" timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL, PRIMARY KEY ("roll_id") , UNIQUE ("id"), UNIQUE ("roll_id"));COMMENT ON TABLE "make"."assemble_rolls" IS E'In this table live the rolls data';
CREATE EXTENSION IF NOT EXISTS pgcrypto;
