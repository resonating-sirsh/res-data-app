ALL_BODY_CONTRACTS = ["VALID_PIECE_NAMES"]


def check_piece_names(body_asset_request, TAG="VALID_PIECE_NAMES"):
    # raise CONTRACT_VALIDATION_EXCEPTION(TAGS)
    return TAG


def check_body(body_asset_request, TAG="BODY_READY"):
    # raise CONTRACT_VALIDATION_EXCEPTION(TAGS)
    # if has all body contracts then ok other wise raise
    return TAG
