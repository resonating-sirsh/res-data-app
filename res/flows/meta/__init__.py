# using this entire file as the node for now
from . import marker as metaone

# this is a type in the new flow api approach
from .marker import MetaMarker
from . import ONE
from . import marker

test = marker
test = metaone
test = MetaMarker
test = ONE
