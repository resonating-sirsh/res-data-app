"""
where we check the meta one for a sku - in 2d or 3d
these can have a status such as pending assets or invalid or good
if pending it may be a human or bot that are creating the assets
"""


def get_meta_one(sku):
    """
    get the garment data - it may be invalid
    """
    return {}


def invalidate_meta_one(sku):
    pass


def invalidate_meta_one_for_body(body, colors=None):
    pass
