### Contract Configuration
from res.utils import logger

ALL_ITEM_CONTRACTS = ["VALID_META_ONE"]

### I need to complete all the gates
def check_meta_one_names(data, TAG="VALID_META_ONE"):
    # raise CONTRACT_VALIDATION_EXCEPTION(TAGS)
    logger.error(data, "Error with contract valid meta one")
    return TAG
