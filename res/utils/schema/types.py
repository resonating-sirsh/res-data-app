"""
removed old sketches starting new -> this may become a res.schema module

- decorators for res_schema, airtable etc.
- classes or properties can take a airtable_schema(name) which will give the base/table of a class or the field of a property
- metric or dimension types can be controlled with a standard decorator or res one 
"""


# def res_schema(
#     obj=None, airtable_name=None, is_metric=False, meta_type=None, is_key=False
# ):
#     pass
