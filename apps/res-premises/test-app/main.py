import time
import pathlib
import os
from crontab import CronTab

# path = __file__.split("apps")[0]

# for root, dirs, files in os.walk(path):
#     if "test-app" in files:
#         print(os.path.join(root, "test-app"))

while True:
    print(79)
    time.sleep(5)
